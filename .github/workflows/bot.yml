name: FB Auto Bot (Video to Release)

on:
  workflow_dispatch: 
    inputs:
      titles:
        description: 'Titles (separated by ,,)'
        required: true
        default: "Real Oviedo vs Villarreal live: LaLiga midnight warzone today,,Watch live: Real Oviedo hunt Villarreal in tonight's Spanish survival epic,,Live now: Real Oviedo vs Villarreal – giants collide under the stars,,LaLiga live: Oviedo host Villarreal in a must-watch destruction at 12:30 am,,Today's live match: Real Oviedo vs Villarreal – all the chaos from Carlos Tartiere"
      descriptions:
        description: 'Descriptions (separated by ,,)'
        required: true
        default: "The clock strikes midnight and Spanish football explodes – catch every bone-rattling tackle, divine save, and match-winning goal as Real Oviedo and Villarreal tear each other apart,,Borja Bastón, Santi Cazorla, and Lucas Ahijado lead Oviedo's furious rebellion, while Gerard Moreno, Álex Baena, and Dani Parejo bring Villarreal's Yellow Submarine artillery – this is primeval LaLiga warfare,,European dreams meet relegation desperation – will Oviedo's fortress devour another giant, or can Villarreal's class silence the Asturian earthquake? Tune in live at 12:30 am,,From the thunderous Carlos Tartiere atmosphere to the tactical assassination on the pitch, this Real Oviedo vs Villarreal clash is pure adrenaline for every football fanatic,,History. Pride. Survival. Glory. Join the live destruction as Real Oviedo and Villarreal write their names in Spanish football flames"
      hashtags:
        description: 'Hashtags (separated by ,,)'
        required: true
        default: "#RealOviedo #Villarreal #LaLiga #MidnightWar ,, #Oviedo #VillarrealCF #CarlosTartiere #LiveFootball ,, #BorjaBaston #GerardMoreno #Cazorla #Parejo ,, #RealOviedoVsVillarreal #SpanishWarfare #LaLigaNights #MatchDay ,, #AsturianEarthquake #YellowSubmarine #watchlive #bulbul4u-live"
  schedule:
    - cron: '0 5 * * *' 

# GitHub ko "Release" banane ki permission dena zaroori hai
permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Code download kar raha hai...
        uses: actions/checkout@v4

      - name: Python setup kar raha hai...
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      - name: System Dependencies (Xvfb & FFmpeg) install kar raha hai...
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb ffmpeg
          pip install DrissionPage requests

      - name: Run Script & Record Video 🎥
        env:
          FB_COOKIES: ${{ secrets.FB_COOKIES }} 
          PYTHONUNBUFFERED: 1  # 👈 YEH LOGS KO REAL-TIME KAREGA
          TITLES: ${{ inputs.titles }}             # 👈 GITHUB UI SE INPUT LERAHA HAI
          DESCRIPTIONS: ${{ inputs.descriptions }} # 👈 GITHUB UI SE INPUT LERAHA HAI
          HASHTAGS: ${{ inputs.hashtags }}         # 👈 GITHUB UI SE INPUT LERAHA HAI
        run: |
          # 1. Virtual screen setup
          Xvfb :99 -screen 0 1920x1080x24 &
          export DISPLAY=:99
          
          # 2. FFmpeg recording start
          ffmpeg -y -video_size 1920x1080 -framerate 30 -f x11grab -i :99.0 -c:v libx264 -preset ultrafast -pix_fmt yuv420p output_video.mp4 > /dev/null 2>&1 &
          FFMPEG_PID=$!
          
          # 3. Apna DrissionPage wala code run karein
          python -u main.py
          
          # 4. Script khatam hone k baad recording safely band karein
          sleep 3
          kill -INT $FFMPEG_PID
          sleep 2

      # ==========================================
      # 🚀 VIDEO KO GITHUB RELEASES MEIN UPLOAD KARNA
      # ==========================================
      - name: Upload Video to GitHub Releases
        if: always() 
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
        run: |
          TAG_NAME="FB-Post-$(date +'%Y%m%d-%H%M%S')"
          
          echo "Uploading output_video.mp4 to GitHub Releases..."
          
          # Check if video was actually created
          if [ -f "output_video.mp4" ]; then
            gh release create "$TAG_NAME" output_video.mp4 \
              --title "Post Upload Saboot: $TAG_NAME" \
              --notes "Automated run ka video evidence."
            echo "✅ Video successfully Releases mein save ho gayi!"
          else
            echo "❌ Video file nahi mili, shayad ffmpeg properly start nahi hua."
          fi


















# DOne dynamic titles desctiotn and hastngs beey bangqay hai aab issko oopper workflow mei input add karta hu taakey assany hu 


# name: FB Auto Bot (Video to Release)

# on:
#   workflow_dispatch: 
#   schedule:
#     - cron: '0 5 * * *' 

# # GitHub ko "Release" banane ki permission dena zaroori hai
# permissions:
#   contents: write

# jobs:
#   run-bot:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Code download kar raha hai...
#         uses: actions/checkout@v4

#       - name: Python setup kar raha hai...
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12' 

#       - name: System Dependencies (Xvfb & FFmpeg) install kar raha hai...
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb ffmpeg
#           pip install DrissionPage requests

#       - name: Run Script & Record Video 🎥
#         env:
#           FB_COOKIES: ${{ secrets.FB_COOKIES }} 
#           PYTHONUNBUFFERED: 1  # 👈 YEH LOGS KO REAL-TIME KAREGA
#         run: |
#           # 1. Virtual screen setup
#           Xvfb :99 -screen 0 1920x1080x24 &
#           export DISPLAY=:99
          
#           # 2. FFmpeg recording start
#           ffmpeg -y -video_size 1920x1080 -framerate 30 -f x11grab -i :99.0 -c:v libx264 -preset ultrafast -pix_fmt yuv420p output_video.mp4 > /dev/null 2>&1 &
#           FFMPEG_PID=$!
          
#           # 3. Apna DrissionPage wala code run karein (-u flag zaroori hai real-time logs ke liye)
#           python -u main.py
          
#           # 4. Script khatam hone k baad recording safely band karein
#           sleep 3
#           kill -INT $FFMPEG_PID
#           sleep 2

#       # ==========================================
#       # 🚀 VIDEO KO GITHUB RELEASES MEIN UPLOAD KARNA
#       # ==========================================
#       - name: Upload Video to GitHub Releases
#         if: always() # 👈 SCRIPT FAIL BHI HO TO VIDEO UPLOAD HOGI TAAYE ERROR DIKH SAKE
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#         run: |
#           TAG_NAME="FB-Post-$(date +'%Y%m%d-%H%M%S')"
          
#           echo "Uploading output_video.mp4 to GitHub Releases..."
          
#           # Check if video was actually created
#           if [ -f "output_video.mp4" ]; then
#             gh release create "$TAG_NAME" output_video.mp4 \
#               --title "Post Upload Saboot: $TAG_NAME" \
#               --notes "Automated run ka video evidence."
#             echo "✅ Video successfully Releases mein save ho gayi!"
#           else
#             echo "❌ Video file nahi mili, shayad ffmpeg properly start nahi hua."
#           fi


          
          





# yeh upload tu kar deya hai lekin 2 post individually measn 2 photo ko eek sath eek post mei uplaod nehy kar pata 



# name: FB Auto Bot (Video to Release)

# on:
#   workflow_dispatch: 
#   schedule:
#     - cron: '0 5 * * *' 

# # GitHub ko "Release" banane ki permission dena zaroori hai
# permissions:
#   contents: write

# jobs:
#   run-bot:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Code download kar raha hai...
#         uses: actions/checkout@v4

#       - name: Python setup kar raha hai...
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12' 

#       - name: System Dependencies (Xvfb & FFmpeg) install kar raha hai...
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb ffmpeg
#           pip install DrissionPage requests

#       - name: Run Script & Record Video 🎥
#         env:
#           FB_COOKIES: ${{ secrets.FB_COOKIES }} 
#           PYTHONUNBUFFERED: 1  # 👈 YEH LOGS KO REAL-TIME KAREGA
#         run: |
#           # 1. Virtual screen setup
#           Xvfb :99 -screen 0 1920x1080x24 &
#           export DISPLAY=:99
          
#           # 2. FFmpeg recording start
#           ffmpeg -y -video_size 1920x1080 -framerate 30 -f x11grab -i :99.0 -c:v libx264 -preset ultrafast -pix_fmt yuv420p output_video.mp4 > /dev/null 2>&1 &
#           FFMPEG_PID=$!
          
#           # 3. Apna DrissionPage wala code run karein (-u flag zaroori hai real-time logs ke liye)
#           python -u main.py
          
#           # 4. Script khatam hone k baad recording safely band karein
#           sleep 3
#           kill -INT $FFMPEG_PID
#           sleep 2

#       # ==========================================
#       # 🚀 VIDEO KO GITHUB RELEASES MEIN UPLOAD KARNA
#       # ==========================================
#       - name: Upload Video to GitHub Releases
#         if: always() # 👈 SCRIPT FAIL BHI HO TO VIDEO UPLOAD HOGI TAAYE ERROR DIKH SAKE
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#         run: |
#           TAG_NAME="FB-Post-$(date +'%Y%m%d-%H%M%S')"
          
#           echo "Uploading output_video.mp4 to GitHub Releases..."
          
#           # Check if video was actually created
#           if [ -f "output_video.mp4" ]; then
#             gh release create "$TAG_NAME" output_video.mp4 \
#               --title "Post Upload Saboot: $TAG_NAME" \
#               --notes "Automated run ka video evidence."
#             echo "✅ Video successfully Releases mein save ho gayi!"
#           else
#             echo "❌ Video file nahi mili, shayad ffmpeg properly start nahi hua."
#           fi









# ================== gret job yeh done huqaaya uplaod succesfuuly done to facebook =====================================================


# name: FB Auto Bot (Video to Release)

# on:
#   workflow_dispatch: 
#   schedule:
#     - cron: '0 5 * * *' 

# # GitHub ko "Release" banane ki permission dena zaroori hai
# permissions:
#   contents: write

# jobs:
#   run-bot:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Code download kar raha hai...
#         uses: actions/checkout@v4

#       - name: Python setup kar raha hai...
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12' 

#       - name: System Dependencies (Xvfb & FFmpeg) install kar raha hai...
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y xvfb ffmpeg
#           pip install DrissionPage

#       - name: Run Script & Record Video 🎥
#         env:
#           FB_COOKIES: ${{ secrets.FB_COOKIES }} 
#         run: |
#           # 1. Virtual screen setup
#           Xvfb :99 -screen 0 1920x1080x24 &
#           export DISPLAY=:99
          
#           # 2. FFmpeg recording start
#           ffmpeg -y -video_size 1920x1080 -framerate 30 -f x11grab -i :99.0 -c:v libx264 -preset ultrafast -pix_fmt yuv420p output_video.mp4 > /dev/null 2>&1 &
#           FFMPEG_PID=$!
          
#           # 3. Apna DrissionPage wala code run karein
#           python main.py
          
#           # 4. Script khatam hone k baad recording safely band karein
#           sleep 3
#           kill -INT $FFMPEG_PID
#           sleep 2

#       # ==========================================
#       # 🚀 VIDEO KO GITHUB RELEASES MEIN UPLOAD KARNA
#       # ==========================================
#       - name: Upload Video to GitHub Releases
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} 
#         run: |
#           TAG_NAME="FB-Post-$(date +'%Y%m%d-%H%M%S')"
          
#           echo "Uploading output_video.mp4 to GitHub Releases..."
          
#           gh release create "$TAG_NAME" output_video.mp4 \
#             --title "Post Upload Saboot: $TAG_NAME" \
#             --notes "DrissionPage ne successfully Facebook par post kar di hai. Screen bilkul clean hai!"
          
#           echo "✅ Video successfully Releases mein save ho gayi!"


















# =========== Good job, facebook open hu raha hai and screenshot saaboot hai beloe code mei, aaabbb opper facebook uplaod waala system add akrty hai inshallah ================



# name: Facebook Auto Bot

# on:
#   workflow_dispatch: 
#   # schedule:
#   #   - cron: '0 5 * * *' 

# jobs:
#   run-script:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Code download kar raha hai...
#         uses: actions/checkout@v4

#       - name: Python setup kar raha hai...
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12' 

#       - name: Selenium install kar raha hai...
#         run: |
#           pip install selenium

#       - name: Bot run kar raha hai!
#         env:
#           FB_COOKIES: ${{ secrets.FB_COOKIES }} 
#         run: python main.py

#       # --- YEH NAYA STEP HAI SABOOT DOWNLOAD KARNE KE LIYE ---
#       - name: Upload Saboot (Screenshots)
#         uses: actions/upload-artifact@v4
#         with:
#           name: FB-Login-Proof
#           path: screenshots/










# name: Facebook Auto Bot

# on:
#   workflow_dispatch: # Yeh aapko GitHub par ek "Run workflow" ka button dega taake aap manually chala sakein
#   schedule:
#     - cron: '0 5 * * *' # Yeh har roz subah 5:00 AM UTC (Pakistan Time: 10:00 AM) ko auto-run hoga

# jobs:
#   run-script:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Code download kar raha hai...
#         uses: actions/checkout@v4

#       - name: Python setup kar raha hai...
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.12' 

#       - name: Selenium install kar raha hai...
#         run: |
#           pip install selenium

#       - name: Bot run kar raha hai!
#         env:
#           # Yahan hum GitHub Secret wali cookies ko as environment variable pass kar rahe hain
#           FB_COOKIES: ${{ secrets.FB_COOKIES }} 
#         run: python main.py
